#!/bin/bash

if ssh aesopos ps -ef | grep -q 'citserver' ; then
	exit 0
elif ssh aesopos ps -ef | grep -q 'citserver' ; then
	exit 0
elif ssh aesopos ps -ef | grep -q 'citserver' ; then
	exit 0
else
	exit 1
fi
